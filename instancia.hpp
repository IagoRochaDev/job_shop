#include <vector>
#include <string>

class Instancia {

    std::vector<std::vector<int>> costs;
    std::vector<std::vector<int>> jobs;

    Instancia() {
        // Inicializar atributos no construtor
        // Por exemplo, definir tamanhos ou valores iniciais se necessário
        // jobs pode ser inicializado com -1, mas sem tamanhos conhecidos, deixamos vazio por enquanto
    }

    void ler_arquivo(std::string arquivo) {
        /*TODO*/
    }
};